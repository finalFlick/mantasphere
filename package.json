{
  "name": "spherestorm",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.cjs",
    "dev": "npx serve . -l 8000",
    "feedback-sync": "node scripts/feedback-sync.js"
  },
  "dependencies": {
    "googleapis": "^134.0.0",
    "@octokit/rest": "^20.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
